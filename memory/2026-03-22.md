# 2026-03-22 — Session Notes

## Major Config Overhaul
Tomer asked to rework the entire OpenClaw session experience. Key changes made:

### 1. Tool Spam Reduction
- Updated AGENTS.md with explicit rules: don't call memory_search, sessions_list, session_status unless actually needed
- Added "Minimize Tool Spam" section to guidelines

### 2. Architecture Upgrade
- **OpenClaw updated** from v2026.2.15 → v2026.3.13 (via npm)
- **OpenAI provider added** (`openai-main`) with GPT-5.4 and GPT-5.4-mini models
- **Tomer's OpenAI API key** configured for: model fallback, embeddings, image understanding
- **Model fallback chain:** Opus 4.6 → GPT-5.4 → Sonnet 4.6
- **Sub-agents:** now use Claude Sonnet 4.6 (faster/cheaper)
- **Image understanding:** GPT-5.4 primary, Opus fallback
- **Auto-update:** enabled, stable channel, checkOnStart=true

### 3. Memory & Session Persistence
- Memory search switched to OpenAI embeddings (text-embedding-3-small)
- Session transcript indexing enabled (experimental.sessionMemory=true)
- memory-lancedb plugin installed with autoCapture + autoRecall
- Sub-agent archive time set to 24h (was instant cleanup)
- Compaction: safeguard mode with memory flush enabled

### 4. Plugins Installed
- **@getfoundry/unbrowse-openclaw** v0.7.12 — preferred web tool
- **@noncelogic/openclaw-memory-lancedb** v0.1.0 — LanceDB long-term memory
- Both installed manually (npm) due to Windows `spawn EINVAL` bug in `openclaw plugins install`

### 5. UI
- Assistant name set to "Claw" in webchat UI

## Travian Reverse Engineering Task
- Tomer wants full documentation of video reward mechanism on https://ts1.x1.europe.travian.com
- Specifically: "+15% for 8 hours" free production bonus (watch video ad)
- Need to capture: client-server communication, ad SDK, completion protocol, anti-cheat, reward tokens
- Reference repo: https://github.com/kalil0321/reverse-api-engineer
- Claude in Chrome attempted this task previously and failed

## Browser Relay Issue (UNRESOLVED)
- OpenClaw Browser Relay extension shows "disconnected (click to re-attach)" with orange error icon
- Relay server IS listening on port 18792 (cdpHttp=true)
- Chrome (PID 74596) has ESTABLISHED connection to 18792
- Root cause: `/json` endpoint returns 401 Unauthorized — extension auth failing
- The extension needs gateway token configured in its options
- **Next step:** Get Tomer to configure the extension with gateway URL + token
- Alternative: launch Chrome with `--remote-debugging-port=9222`

## Windows exec Tool Quirk
- Non-PTY exec commands return empty output after gateway restarts
- PTY mode (`pty: true`) works reliably — use this as default on Windows

## Tomer's API Keys (configured in openclaw.json)
- 3 Anthropic auth profiles (team seats)
- 1 OpenAI API key (for GPT-5.4, embeddings, vision)
- Has 2 Claude Team users + 2 ChatGPT Premium users total

## Current Model Landscape (March 2026)
- Anthropic: Opus 4.6, Sonnet 4.6, Haiku 4.5
- OpenAI: GPT-5.4, GPT-5.4-mini, GPT-5.4-nano
